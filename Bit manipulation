int count_one (int n)
{
    while(n)
    {
        n = n & (n-1);
        count++;
    }
    return count;
}

// Complexity: O(K), where K is the number of ones present in the binary form of the given number.
// Above was actually a log(N) problem with base 2.

